name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

# Cancel duplicate runs on the same PR/branch — saves CI minutes when
# pushing several commits in quick succession.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Test, typecheck and build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        # Skip the Electron binary download — CI only runs tests + build, never
        # packages the desktop app, so the ~100MB binary is dead weight here.
        env:
          ELECTRON_SKIP_BINARY_DOWNLOAD: '1'
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Build
        # Dummy env vars are enough for `next build` — the real values are
        # only read at request time. Using fakes keeps real secrets out of
        # CI and avoids needing to wire up GitHub Secrets for every fork.
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://ci-placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-anon-key
          OFFLINE_SESSION_SECRET: ci-placeholder-offline-secret
        run: npm run build
